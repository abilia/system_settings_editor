class CrossOverLayout {
  final double strokeWidth;

  const CrossOverLayout({
    this.strokeWidth = 2,
  });
}

class CrossOverLayoutMedium extends CrossOverLayout {
  const CrossOverLayoutMedium() : super(strokeWidth: 3);
}
