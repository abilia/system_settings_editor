import 'package:flutter/material.dart';

class TimePillar extends StatelessWidget {
  const TimePillar({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('YOOOOO TIMEPILLAR!!!!'),
    );
  }
}
