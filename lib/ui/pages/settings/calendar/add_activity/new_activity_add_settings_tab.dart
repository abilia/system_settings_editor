import 'package:seagull/ui/all.dart';

class NewActivityAddSettingsTab extends StatelessWidget {
  const NewActivityAddSettingsTab({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
