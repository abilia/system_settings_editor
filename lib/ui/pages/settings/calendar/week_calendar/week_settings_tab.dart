import 'package:flutter/material.dart';

class WeekSettingsTab extends StatelessWidget {
  const WeekSettingsTab({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('week settings tab'),
    );
  }
}
